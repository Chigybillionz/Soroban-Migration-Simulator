[package]
name = "storage-analyzer"
version = "0.1.0"
edition = "2021"

[dependencies]
analyzer = { path = "../analyzer" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
